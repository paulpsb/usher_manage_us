<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.InternalExamsPassageVocaDao">
	<select id="getPassageVocaList" resultType="com.usher.dto.InternalExamsPassageVocaDto" parameterType="com.usher.dto.InternalExamsPassageVocaDto">
		select 
			id,
			spell,
			meaning,
			passage_id,
			paragraph,
			question_num
		from
			internal_exams_passage_voca
		where 
			passage_id = #{passage_id}
		order by question_num, id
	</select>

	<insert id="insertPassageVoca" parameterType="com.usher.dto.InternalExamsPassageVocaDto">
		insert into internal_exams_passage_voca (
			created,
			modified,
			spell,
			meaning,
			passage_id,
			paragraph,
			question_num
		) values (
			now(),
			now(),
			#{spell},
			#{meaning},
			#{passage_id},
			#{paragraph},
			#{question_num}
		)
	</insert>
	
	<update id="updatePassageVoca" parameterType="com.usher.dto.InternalExamsPassageVocaDto">
		update internal_exams_passage_voca set
			modified = now(),
			spell = #{spell},
			meaning = #{meaning},
			paragraph = #{paragraph},
			question_num = #{question_num}
		where
			id = #{id}	
	</update>
	
	<delete id="deletePassageVoca" parameterType="com.usher.dto.InternalExamsPassageVocaDto">
		delete from internal_exams_passage_voca
		where
			id = #{id}	
	</delete>
	
</mapper>