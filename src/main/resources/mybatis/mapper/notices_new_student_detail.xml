<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.NoticesNewStudentDetailDao">
	<select id="getNoticesNewStudentDetailList" resultType="com.usher.dto.NoticesNewStudentDetailDto" parameterType="com.usher.dto.NoticesNewStudentDetailDto">
		select
			id,
			course_enrollment_id,
			user_id,
			student_name,
			student_level,
			check_orientation,
			training_desc,
			is_training_desc,
			notice_new_student_id
		from
			notices_new_student_detail
		where
			notice_new_student_id = #{notice_new_student_id}
		order by notice_new_student_id, student_name
	</select>	
	<update id="updatNewStudentTraining" parameterType="com.usher.dto.NoticesNewStudentDetailDto">
		update notices_new_student_detail set 
			modified = now(),
			training_desc = #{training_desc},
			is_training_desc = #{is_training_desc}
		where
			notice_new_student_id = #{notice_new_student_id}
			and course_enrollment_id = #{course_enrollment_id}
	</update>
</mapper>