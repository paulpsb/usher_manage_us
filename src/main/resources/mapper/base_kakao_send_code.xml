<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.BaseKakaoSendCodeDao">
	<select id="getBaseKakaoSendCodeList" resultType="com.usher.dto.BaseKakaoSendCodeDto">
		select
			a.id,
			to_char(a.created,'yyyy-mm-dd hh24:mi:ss') as created,
			a.created_id,
			concat(coalesce(b.last_name,''),coalesce(b.first_name,'')) as created_name,
			to_char(a.modified,'yyyy-mm-dd hh24:mi:ss') as modified,
			a.modified_id,
			concat(coalesce(c.last_name,''),coalesce(c.first_name,'')) as modified_name,
			a.send_code,
			a.send_value,
			a.send_default_value
		from
			base_kakao_send_code a
			left join auth_user b on a.created_id = b.id
			left join auth_user c on a.modified_id = c.id
		order by a.id
	</select>

	<insert id="insertBaseKakaoSendCode" parameterType="com.usher.dto.BaseKakaoSendCodeDto">
		insert into base_kakao_send_code(
			created,
			created_id,
			modified,
			modified_id,
			send_code,
			send_value,
			send_default_value
		) values (
			now(),
			#{created_id},
			now(),
			#{modified_id},
			#{send_code},
			#{send_value},
			#{send_default_value}
		)
	</insert>	
	<update id="updateBaseKakaoSendCode" parameterType="com.usher.dto.BaseKakaoSendCodeDto">
		update base_kakao_send_code set
			modified = now(),
			modified_id = #{modified_id},
			send_value = #{send_value},
			send_default_value = #{send_default_value}
		where
			id = #{id}
	</update>

	<delete id="deleteBaseKakaoSendCode" parameterType="com.usher.dto.BaseKakaoSendCodeDto">
		delete from base_kakao_send_code
		where
			id = #{id}
	</delete>	
</mapper>