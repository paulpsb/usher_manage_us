<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.EnrollmentsOrientationenrollmentDao">
	<select id="getEnrollmentsOrientationList" parameterType="com.usher.dto.EnrollmentsOrientationenrollmentDto" resultType="com.usher.dto.EnrollmentsOrientationenrollmentDto">
		select
			course_enrollment_id,
			orientation_code,
			orientation_accept,
			orientation_reason
		from
			enrollments_orientationenrollment
		where
			course_enrollment_id in ( select id from enrollments_courseenrollment where course_id = #{course_id} )
		order by id
	</select>
</mapper>