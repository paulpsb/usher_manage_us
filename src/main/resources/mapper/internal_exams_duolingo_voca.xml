<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.InternalExamsDuolingoVocaDao">
	<select id="getExamsDuolingoVocaList" resultType="com.usher.dto.InternalExamsDuolingoVocaDto" parameterType="com.usher.dto.InternalExamsDuolingoVocaDto">
		select
			a.id,
			to_char(a.created,'yyyy-mm-dd hh24:mi:ss') as created,
			a.created_id,
			concat(coalesce(b.last_name,''),coalesce(b.first_name,'')) as created_name,
			to_char(a.modified,'yyyy-mm-dd hh24:mi:ss') as modified,
			a.modified_id,
			concat(coalesce(c.last_name,''),coalesce(c.first_name,'')) as modified_name,
			a.status,
			a.section,
			a.book,
			a.volume,
			a."group",
			a.article,
			a.voca_lang_code,
			a.voca_word,
			a.voca_word_translation,
			a.voca_word_sort
		from
			internal_exams_duolingo_voca a
			left join auth_user b on a.created_id = b.id
			left join auth_user c on a.modified_id = c.id
		where
			a.section = #{section}
			and a.book = #{book}
			and a.volume = #{volume}
			and a."group" = #{group}
			and a.article = #{article}
		order by a.voca_word_sort
	</select>
	
	<insert id="insertDuolingoVoca" parameterType="com.usher.dto.InternalExamsDuolingoVocaDto" useGeneratedKeys="true" keyProperty="id" >
		insert into internal_exams_duolingo_voca (
			created,
			created_id,
			modified,
			modified_id,
			status,
			section,
			book,
			volume,
			"group",
			article,
		    voca_lang_code,
		    voca_word,
		    voca_word_translation,
		    voca_word_sort
		) values (
			now(),
			#{created_id},
			now(),
			#{modified_id},
			#{status},
			#{section},
			#{book},
			#{volume},
			#{group},
			#{article},
		    #{voca_lang_code},
		    #{voca_word},
		    #{voca_word_translation},
		    #{voca_word_sort}
		)
	</insert>	
	
	<update id="updateDuolingoVoca" parameterType="com.usher.dto.InternalExamsDuolingoVocaDto">
		update internal_exams_duolingo_voca set
			modified = now(),
			modified_id = #{modified_id},
			status = #{status},
			voca_lang_code = #{voca_lang_code},
			voca_word = #{voca_word},
			voca_word_translation = #{voca_word_translation},
			voca_word_sort = #{voca_word_sort}
		where
			id = #{id}
	</update>
		
	<delete id="deleteDuolingoVoca" parameterType="com.usher.dto.InternalExamsDuolingoVocaDto">
		delete from internal_exams_duolingo_voca
		where
			id = #{id}
	</delete>			
</mapper>