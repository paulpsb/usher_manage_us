<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.MockTestDirectionDao">
	<select id="getMockTestDirection" resultType="com.usher.dto.MockTestDirectionDto" parameterType="com.usher.dto.MockTestDirectionDto">
		select
			id,
			section,
			title,
			direction,
			usher_title,
			usher_direction
		from 
			mock_test_direction 
		where
			section = #{section}
	</select>

	<insert id="insertMockTestDirection" parameterType="com.usher.dto.MockTestDirectionDto">
		insert into mock_test_direction(
			created,
			modified,
			section,
			title,
			direction,
			usher_title,
			usher_direction
		) values (
			now(),
			now(),
			#{section},
			#{title},
			#{direction},
			#{usher_title},
			#{usher_direction}
		)
	</insert>	

	<update id="updateMockTestDirection" parameterType="com.usher.dto.MockTestDirectionDto">
		update mock_test_direction set
			title = #{title},
			direction = #{direction},
			usher_title = #{usher_title},
			usher_direction = #{usher_direction}
		where
			id = #{id}
	</update>	
</mapper>