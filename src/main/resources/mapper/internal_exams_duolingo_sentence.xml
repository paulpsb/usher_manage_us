<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.InternalExamsDuolingoSentenceDao">
	<select id="getExamsDuolingoSentenceList" resultType="com.usher.dto.InternalExamsDuolingoSentenceDto" parameterType="com.usher.dto.InternalExamsDuolingoSentenceDto">
		select
			a.id,
			to_char(a.created,'yyyy-mm-dd hh24:mi:ss') as created,
			a.created_id,
			concat(coalesce(b.last_name,''),coalesce(b.first_name,'')) as created_name,
			to_char(a.modified,'yyyy-mm-dd hh24:mi:ss') as modified,
			a.modified_id,
			concat(coalesce(c.last_name,''),coalesce(c.first_name,'')) as modified_name,
			a.status,
			a.section,
			a.book,
			a.volume,
			a."group",
			a.article,
			a.sentence_doc,
			a.sentence_sort
		from
			internal_exams_duolingo_sentence a
			left join auth_user b on a.created_id = b.id
			left join auth_user c on a.modified_id = c.id
		where
			a.section = #{section}
			and a.book = #{book}
			and a.volume = #{volume}
			and a."group" = #{group}
			and a.article = #{article}
		order by a.sentence_sort
	</select>
	
	<insert id="insertDuolingoSentence" parameterType="com.usher.dto.InternalExamsDuolingoSentenceDto" useGeneratedKeys="true" keyProperty="id" >
		insert into internal_exams_duolingo_sentence (
			created,
			created_id,
			modified,
			modified_id,
			status,
			section,
			book,
			volume,
			"group",
			article,
		    sentence_doc,
		    sentence_sort
		) values (
			now(),
			#{created_id},
			now(),
			#{modified_id},
			#{status},
			#{section},
			#{book},
			#{volume},
			#{group},
			#{article},
		    #{sentence_doc},
		    #{sentence_sort}
		)
	</insert>	
	
	<update id="updateDuolingoSentence" parameterType="com.usher.dto.InternalExamsDuolingoSentenceDto">
		update internal_exams_duolingo_sentence set
			modified = now(),
			modified_id = #{modified_id},
			status = #{status},
			sentence_doc = #{sentence_doc},
			sentence_sort = #{sentence_sort}
		where
			id = #{id}
	</update>
		
	<delete id="deleteDuolingoSentence" parameterType="com.usher.dto.InternalExamsDuolingoSentenceDto">
		delete from internal_exams_duolingo_sentence
		where
			id = #{id}
	</delete>			
</mapper>