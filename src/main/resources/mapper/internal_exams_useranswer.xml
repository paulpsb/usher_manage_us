<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usher.dao.InternalExamsUseranswerDao">
	<select id="getExamUserAnswer" parameterType="com.usher.dto.InternalExamsUseranswerDto" resultType="com.usher.dto.InternalExamsUseranswerDto">
		select 
			id,
			to_char(created,'yyyy-mm-dd hh24:mi:ss') as created,
			to_char(modified,'yyyy-mm-dd hh24:mi:ss') as modified,
			answer,
			result,
			practice_result_id
		from 
			internal_exams_useranswer
		where
			practice_result_id = #{practice_result_id}

	</select>
</mapper>